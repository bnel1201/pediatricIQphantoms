# paths are relative to the main directory
OUTPUT_DIR=/home/brandon.nelson/Dev/Regulatory_Science_Tools/pediatricIQphantoms/test #replace with rel path when in one config file
RESULTS_DIR=$OUTPUT_DIR/results

BASEDIR=$(dirname "$0")
echo $(dirname $BASEDIR)
# Do not edit below
mkdir -p $OUTPUT_DIR $RESULTS_DIR
RESULTS_DIR=$(realpath $RESULTS_DIR)
OUTPUT_DIR=$(realpath $OUTPUT_DIR)